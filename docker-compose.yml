# Python Backend - Docker Compose for Local Development

version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
      PORT: 5000
    volumes:
      - ./python-backend:/app
    command: python app.py
